### WHAT IS THIS?

>'U-Gdscript' (or 'user gd script') is a theoretical work in progress way to load user provided strings as usable code with error checking. ideally this is done without recompilation or godot extensions

### WHATS UR PROGRESS?

>im like half way there!! code generation exists and technically works, BUT im stuck on figuring out a way to error check the actual code past the lexical/tokenization stage without major restructuring.
